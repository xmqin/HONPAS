Basis generation (Actually, KB projectors and PAO generation)

A subdirectory for each series of examples. 

Instructions:

(We assume that you did not move this directory from its place
in the Siesta distribution...)

Go into each subdirectory and use the gen-basis.sh script above. For
example:

$ cd Si
$ sh ../gen-basis.sh Si_sz.fdf Si.tm2.psf

Then go into the working subdirectory and use the plotting scripts,
as per the instructions printed by the script:

$ cd Si_sz

.... Plot using the scripts ....


