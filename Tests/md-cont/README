In this test, the h2o.VERLET_RESTART file is used to continue the
simulation almost seamlessly. (Compare this concatenation of runs with
the one in the md-single-run directory).

The easiest way is probably to plot the information in the .MDE file.


